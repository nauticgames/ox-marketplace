import React from "react";
import StyledDisabledButton from "./styles";

const NotAuthenticatedButton = () => {
  return <StyledDisabledButton>Sign in to buy</StyledDisabledButton>;
};

export default NotAuthenticatedButton;
